# Generated by "mkproy.sh" v0.3
CMAKE_MINIMUM_REQUIRED (VERSION 2.6)
SET(MODULENAME spinnakerInterface)

# Start a project
PROJECT(${MODULENAME})

# Find YARP.  Point the YARP_DIR environment variable at your build.
#FIND_PACKAGE(YARP REQUIRED)
#FIND_PACKAGE(OpenCV REQUIRED)
FIND_PACKAGE(eventdriven)

set(MY_SRC_PATH ${CMAKE_CURRENT_SOURCE_DIR})

# Search for source code.
FILE(GLOB folder_source ${MY_SRC_PATH}/src/*.cpp)
FILE(GLOB folder_header ${MY_SRC_PATH}/include/*.h)
SOURCE_GROUP("Source Files" FILES ${folder_source})
SOURCE_GROUP("Header Files" FILES ${folder_header})


INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/include
                    ${YARP_INCLUDE_DIRS}
                    ${EVENTDRIVENLIBS_INCLUDE_DIRS}
                    ${SPINNAKER_IO_INCLUDE_DIRS})


set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wall -fno-strict-aliasing -std=c++11 -fPIC")
ADD_EXECUTABLE(${MODULENAME} ${folder_source} ${folder_header})
TARGET_LINK_LIBRARIES(${MODULENAME} ${OpenCV_LIBS} ${YARP_LIBRARIES} eventdriven ${SPINNAKER_IO_LIB}  sqlite3 pthread)

#INSTALL(TARGETS ${MODULENAME} DESTINATION bin)
yarp_install(TARGETS ${MODULENAME} DESTINATION bin)
